# Changelog

## Unreleased

### Dead-code removal — stale Zen fragments referencing pre-overhaul DOM
- Removed `HiveCompactMode` (a `ZenCompactMode.mjs` port) and its `init()` call: it tracked `data-compact-mode` and referenced `#sidebar`/`#toolbar`/`#webviewContainer`, none of which exist in the post-overhaul DOM. The real compact mode is `body[data-compact]` via `setCompactMode` (which uses `#chrome`), so this tracker only ever ran `init()` and its `mouseleave`/`sizemodechange` listeners no-op'd on missing elements.
- Removed `HiveQuickCommands` (a Vivaldi F2-style palette) plus its `#quick-commands` CSS: `toggle()`/`filter()` were never invoked and the `#quick-commands` element doesn't exist.
- Removed the dead `#chrome__top` CSS — the "Zen Single Toolbar Mode" and "Zen Floating URL Bar" sections styled an element that was never ported, under a `data-layout="toolbar"` attribute the JS never sets (tab layout is `data-mode` = vertical/horizontal).
- `webviewContainer` lookups are already guarded (fallback to `document.body` / early return), so the native CEF webview living outside the chrome DOM is safe and left in place.

### Fix — light/dark theme switch flashed dark-on-dark
- The body carried a vestigial `transition: background-color 0.4s ease` (labeled "workspace transition"), but the workspace color wash is actually applied via `#chrome::before` (opacity + gradient) and `data-workspace-color` is never set by JS. The rule therefore only fired on theme switches: the body's background faded over 0.4s while its text color flipped instantly, so dark→light showed dark text `#191817` on a still-dark `#1A1512` canvas for the first ~0.4s (cream-on-light in the other direction). Removed the dead rule so background and text flip together, instantly.
- Verified at runtime over the CDP debug bridge: the body background and foreground now resolve in lockstep — dark `#1A1512`/`#F4F1EE` → light `#F7F2E8`/`#191817` — with no transition artifact.

### Dead-code removal — duplicate bridge aliases
- Removed three never-invoked bridge registrations that duplicated live handlers: `hive.goBack`/`hive.goForward` (the web chrome calls `hive.back`/`hive.forward`) and `hive.togglePin` (it calls `hive.pinTab`). Both directions of the bridge contract are now clean — every `api()` call in the chrome is registered, and no registered handler is a redundant alias.

### Fix — site permissions panel now reflects real per-site state
- The Site Permissions palette rendered every toggle as OFF because `state.sitePermissions` was never sent (the field existed in the JS but not the bridge DTO). Added a per-active-host `sitePermissions` map (kind → `allow`/`deny`/`ask`) to `WebChromeStartData`, empty on private pages, so camera/microphone/location/notifications/pop-ups toggles now show the persisted decision.
- Removed the dead "Autoplay" toggle row — the native layer explicitly no-ops `autoplay` (engine-level, not a per-site decision in this build), so the control was misleading.

### Fix — reading list panel now shows saved articles
- The Reading List palette always rendered "Your reading list is empty" because `state.readingList` was never sent: the field existed in the JS renderer but not in the `WebChromeStartData` bridge DTO. Added `readingList` (id/title/url/host, newest first) populated from the native `BrowserState.readingList`, threaded through both redaction paths (empty on private start pages), so saved articles now appear in the panel.

### Feature — wire up the "Recently Closed" new-tab section (Chrome NTP parity)
- The JS renderer for a "Recently Closed" section existed, and `hive.reopenClosedTab` (⌘⇧T) was wired, but the section **never rendered**: the HTML container was missing and the state DTO never carried `recentlyClosed`. Added the field to `WebChromeStartData` (populated from `BrowserState.closedTabs`, newest first, blank/internal URLs filtered), threaded it through both redaction paths (empty on private start pages), and added the NTP section so closing a tab now surfaces a "Reopen" row on the new-tab page.

### Fix — undefined legacy tokens were silently blanking chrome text and surfaces
- A whole pre-overhaul token set was referenced by dozens of components but **never defined anywhere** — `--ink` (24 refs), `--ink-secondary` (14), `--mist` (6), `--paper` (3), `--chrome-bg` (3), `--text-primary`/`--text-secondary`/`--text-tertiary`, `--surface-hover-subtle`, `--border`, `--shadow-lg`, `--success`/`--warning`, and the `--ease-out-quick`/`--ease-in-exit` easings. Because CSS treats an undefined `var()` as invalid-at-computed-value-time, every rule using them fell back to `unset` — the sidebar and sidecar had **no background**, split-view tiles and the quick-commands input had **no paper surface**, and text reverted to inherited color instead of the warm cream ramp.
- Added a legacy-alias block to both theme layers (dark + light) mapping each name to its canonical warm-ramp token (`--ink`→`--text`, `--paper`→`--bg-elev`, `--chrome-bg`→`--bg`, `--mist`→`--text-faint`, `--border`→`--hairline-strong`, `--shadow-lg`→`--shadow-2`, `--success`→`#34D399`, `--warning`→`#FBBF24`) plus `--ease-out-quick`/`--ease-in-exit` in the motion block. Per-tab/geometry tokens (`--arc-*`, `--tab-*`, `--ws-color-*`, `--workspace-gradient*`, `--data-theme-color`) were intentionally left alone — they are set inline at runtime, not global theme tokens.

### Fix — Zen accent-mixer chain was broken by a missing token
- The Zen-derived theme layer in `tokens.css` resolved `--zen-primary-color` from `var(--color-accent-1)`, but that token was never defined — so `--zen-colors-primary` / `--zen-colors-secondary` / `--zen-colors-tertiary` (and everything derived from them, including the workspace drop-target glow in `styles.css`) collapsed to invalid values. Added the missing `--color-accent-1: rgb(var(--color-accent))` full-color honey token so the `color-mix()` chain has a real color to blend from.

### Fix — web chrome surfaces aligned to the warm honey ramp
- The web chrome's base palette was still the pre-overhaul cool gray (`--bg: #17171B`, `--text: #EDEDF1`, cool light paper) while the brand — and `tokens.css` / `HiveDesign.swift` — specify a warm editorial ramp (`#1A1512` canvas, `#F4F1EE` cream, `#F7F2E8` paper). Reconciled both dark and light themes to the warm ramp so the chrome matches the native panels instead of reading as a translucent cool-gray utility.
- Corrected the SigmaOS magic-theme `--page-accent` fallback from amber `#F5A623` to honey `#F97316`.

### Fix — web chrome honey accent was silently dropped by malformed CSS
- The web chrome's `--accent: #F97316`, spring motion curves, and ambient bloom were declared in a **malformed block** — the opening `:root {` was missing, so every one of those custom properties was invalid and ignored by the renderer. The dark chrome had no CSS accent at all, and the light theme still carried the pre-overhaul **Arc violet `#7A4ED4`** accent. Wrapped the orphaned declarations in a proper `:root { }`, switched the light theme to honey, removed the violet-tinted `--accent-ink`, and corrected the "Arc violet" header comment.

### Fix — brand accent aligned to honey (#F97316) across every surface
- The brand accent had drifted after the U1 "honey #F97316" decision: the native adaptive accent (`HiveBrand.accentHex`) defaulted to amber `#F5A623`, the `HiveColorToken` system still returned `#FFB84D`/`#9A5A00` amber, the first theme preset was "Hive Amber", and the web chrome fell back to indigo `#6366F1` — while the documented accent, `HiveBrand.accent`, `tokens.css`, and `browserAccentColorHex` all used honey. Every surface now resolves to honey `#F97316` (dark) / `#9A4A0D` (light).
- **Fixed a relaunch bug** where the user's saved accent color was restored to the browser chrome (`browserAccentColorHex`) but never re-synced to `HiveBrand.accentHex`, so native SwiftUI views fell back to the default honey after every launch and diverged from the saved theme.
- Amber `#F5A623` remains a **selectable color option** for profiles, workspaces, tab groups, and skills — it is no longer the brand default.

### Dead-code removal (unwired embedding/semantic-search scaffold)
- **Removed the dead `embedder` model role end to end** — the `.embedder` `ModelRole` case, its `ModelManifest` entry, the `.systemEmbedder` `ServingStrategy`/`Provider` cases, the `MockRuntime` branch, and the 560 MB `nomic-embed-text-v2` entry in `ModelDownloader.requiredRepos`. Nothing ever dispatched to it, and Honeycomb retrieval is FTS5 + graph, so the "semantic index" it claimed to power did not exist.
- **Deleted `EmbeddingRuntime.swift`** — `SystemEmbeddingRuntime` (Apple `NLEmbedding`), the `EmbeddingRuntime` protocol, and `EmbeddingError` were never instantiated in production (only their own test referenced them).
- **Corrected the false claims** — the Dispatcher's "`.systemEmbedder` handled separately via EmbeddingRuntime" comment, the `ModelManifest` "outputs embeddings for Honeycomb semantic index + retrieval" doc, and the YC pitch's "hybrid (full-text + semantic + recency)" retrieval claim (now "full-text + recency").
- Role count 21 → 20. Suite is now **1,782 tests / 174 suites**.

### Dead-code removal (unwired ambient-capture role)
- **Removed the `captureScribe` model role end to end** — the last trace of the ambient-capture roadmap: the `ModelRole.captureScribe` case, its `ModelManifest` entry, the `CellPromptLoader.cellRoleMapping` entry, and the `MockRuntime` branch. It was already unreachable after `ScribeCoordinator` / `PageCapture*` were removed, and it no-oped at the honest mock. Role count 22 → 21, mapped-role count 19 → 18.
- **Deleted `scribe/100m_capture_scribe.md`** and updated the Swarm prompt canon to match: the packaged-tree count (60 → 59), the specialist-Cell count (35 → 34), and the `00_INDEX.md` / `VISION_SPEC.md` / `00_PROGRESS.md` references all dropped the removed Cell. The historical `Pass 43` changelog entry is preserved as a dated record.

### Dead-code removal (unused extensions toolbar)
- **Removed `ExtensionsToolbar`** — the dead "pinned extension icons next to the address bar" view; the live extension UI is `ExtensionsManagerSheet`. The README project-structure listing and the app-file count (168 → 167) were corrected to match.

### Dead-code removal (unused session persistence + sidecar DTOs)
- **Removed `SessionPersistenceManager`** — a second session-persistence actor (save/load/clear plus a crash counter) that was never instantiated; the live path is `BrowserSessionStore` / `SessionRestoration`.
- **Removed the vestigial `SessionSnapshot` struct** from `SessionStore.swift` (superseded by `SessionRecord`) and corrected its stale comment.
- **Removed the unwired `WebChromeSidecarStep` / `WebChromeSidecarChain` DTOs** — sidecar bridge payloads that no handler ever decoded.

### Dead-code removal (unwired ambient-capture machinery)
- **Removed `ScribeCoordinator` + the `PageCapture*` policies** — the tested but never-wired ambient-capture machinery (`autoCaptureTriage` / `askOnPage` invocation routes, `PageCaptureAdmission`, `PageCaptureDeliveryPolicy`, `PageCaptureRequestLedger`). Capture is the honest one-click feature the copy already describes; the "capture every page automatically" path this code implements was never hooked to any page-load event and would run counter to the zero-cost free tier. Four source files and 46 tests (4 suites) removed; the live page-Q&A route through `SwarmOrchestrator` / `ModelRole.pageQa` is untouched.
- Suite is now **1,783 tests / 175 suites**.

### Dead-code removal (superseded T0 intent classifier)
- **Removed `IntentOrchestrator` + `IntentCategory` / `ClassifiedIntent` / `IntentParams`** — a 510-line deterministic T0 intent classifier that request routing no longer uses. Live routing flows through `SwarmIntentRouter`, `TrustedTurnGateway`, and `VoiceCommandCoordinator` (which use `SwarmResponseRoute` and `ModelRole` instead), so this whole family was dead code validated only by its own smoke tests. Its 6 test functions went with it.
- Suite is now **1,829 tests / 179 suites**.

### Dead-code removal (Bee job queue + duplicate profile system)
- **Removed the `BeeQueue` / `BeeJob` job-orchestration subsystem** — an in-memory actor queue (enqueue/start/cancel/retry/verify + auto-retry) with an EventLedger integration that was fully implemented and tested but never instantiated in production. The plan docs themselves flagged it as "code-present but not a durable Flow runtime" (in-memory only, jobs lost on restart, honest-stub `perform` methods). ~14 KB of dead code plus its 25 tests are gone.
- **Removed the duplicate `BrowserProfile` + `ProfileManager` system** — a second profile model with per-profile data directories (`~/Library/Application Support/Hive/Profiles/<id>/`) that no code path ever used; the app's live profile feature is `BrowserState.Profile`, which persists in the session envelope. Its one test went with it.
- **Renamed the trailing test container** — the file's last `@Suite` was mislabeled `BeeQueueTests` even though it held hundreds of unrelated enum/type smoke tests; it is now `SmokeTests`.
- **Corrected a stale `WebSearchProvider` comment** that referenced the removed `Bee` queue.
- Suite is now **1,835 tests / 179 suites**.

### Dead-code removal (unwired persistence layer)
- **Removed a full Chrome-preference persistence layer that was built and tested but never wired** — `ChromeUserPrefs` (the preferences struct), `ChromePrefsStore` (its disk store), and `BrowsingHistoryEntry` (an unused history model). None had a single production reference; the app persists its own session envelope and never read these types.
- **Extracted the live pieces before deleting** — `UserDefinedCommand` (used by the command palette) moved out of `ReadingListEntry.swift` into its own file, and the live `hiveReadingListCap` capability constant was preserved.
- **Removed the dead `ClosedTabRecord` type** and its `BrowserTab` extension.
- **Corrected seven false "Persisted in ChromeUserPrefs" comments** across `BrowserProfile`, `SearchEngine`, `SitePermissions`, `Bookmark`, `PinnedWebApp`, `BrowserImportEngine`, and `BrowserSessionStore` — these types persist in the app's own session envelope, not the never-wired `ChromeUserPrefs`.
- **Removed 20 dead tests (2 suites)** that validated the deleted types; the suite is now **1,861 tests / 179 suites**.

### Dead-code removal (vestigial duplicate implementations)
- **Removed ~870 lines of dead code** — vestigial duplicate implementations that shipped fully-implemented and unit-tested but were never wired: `PageContextBroker` (an async alternative to the live synchronous `buildPageContext()`) and the `SwarmResearchSession` / `SwarmResearchReducer` / `SwarmResearchPresentation` family (superseded by the wired `DeepResearchPlanner` + `ResearchHandoffCoordinator`). Each had zero production references, so they added compile weight, a duplicate `PageContext` definition, and 31 tests that validated code nothing ever ran.
- **The live `PageContext` struct was preserved** — it moved to its own `PageContext.swift` (the shared snapshot used by Swarm, capture, Honeycomb, and context assembly); only the dead broker class was deleted.
- **`ModelCouncilV2.swift` and `IntentOrchestrator` were intentionally left alone** — the former is misnamed but live (it defines the production `ModelCouncil`), and the latter's `IntentCategory` is still referenced by tests.

### Bridge completeness + shipped-chrome sync + honest surface pass
- **19 dead web-chrome buttons now work.** `app.js` called `hive.findInPage`, `hive.savePassword`, `hive.translatePage`, `hive.viewSource`, `hive.downloadURL`, `hive.respondPermission`, `hive.addBookmark`, and a dozen more that Swift never registered — each silently no-opped. All are now wired to real native behaviors (`BrowserState+WebChromeSurface.swift`), and `everyBridgeCallFromWebChromeIsRegistered` now passes (it was the suite's single failing test).
- **The shipped binary was running a stale chrome.** `WebChromeAssets.swift` (generated by `scripts/embed_webchrome.py`) had drifted from source — the app shipped a 138 KB `app.js` while source was 226 KB. Assets are re-embedded so the binary matches source byte-for-byte.
- **Removed particle/confetti/ambient slop** from the web chrome (dead canvas + inert `.ambient`/`.glow` atmosphere) and the native particle field behind every page; replaced the emoji restore icon with an SVG glyph.
- **MLX no longer hard-kills the process.** mlx-c's default error handler calls `exit(-1)`; a process-wide non-exiting handler (`MLXErrorSafety.swift`) now logs instead, and `build-hive-app.sh` ships `mlx-swift_Cmlx.bundle` (with `default.metallib`) so on-device inference actually loads in the packaged app.
- **Landing page is honest** — no fake waitlist counter, no fake arch-split download links, no "Windows/iOS coming soon" (macOS only). MIT `LICENSE` added (README/terms already claimed it).
- **Stopped shipping ~1 MB of AI scaffolding** — the `Swarm_System_Prompts/` progress logs, spec files, and competitor dossiers stay out of the bundle; only the 11 loader-referenced role subdirectories (~492 KB) ship.
- **Swarm AI is now wired to its prompts.** `SwarmOrchestrator` was constructed with `prompts: nil`, so the retrieval-ranker and librarian steps were dead and every Cell ran with an empty system prompt. `setupAI()` now builds a `CellPromptLoader` over `Bundle.module.resourceURL` and passes it in, lighting up ranking, librarian extraction, and per-role system prompts.
- **Renderer crash recovery is now wired.** CEF has always fired `renderProcessDidTerminate`, but no one overrode the no-op default, so a renderer crash left a silently hung page. `CefWebViewModel` now exposes `onRendererTerminated`, and `BrowserState` feeds it through the engine-agnostic `RendererRecoveryController` (the crash-loop policy that was implemented and tested but never instantiated): one crash triggers a single bounded auto-reload; a crash loop (three crashes in five minutes) surfaces an honest "This page crashed" banner with Reload instead of a silent hang.
- **BYOK (Bring Your Own Key) is now actually wired.** `BYOKRuntime` was fully implemented and tested, but `setBYOK` was never called and there was no way to configure a gateway — so the assistant's "Remote (BYOK)" toggle silently fell through to the honest mock. A new Settings → Performance → **Remote AI (BYOK)** group lets you enter a LiteLLM/OpenAI-compatible base URL, model ID, and API key (Keychain-backed, like Tavily); `configureBYOK()` builds the runtime and installs it on `Dispatcher.shared` at startup and on every change, so the toggle routes to a real model. You pay the provider — Hive pays nothing.
- **Capture claims are now honest.** The landing page and Product Hunt page promised "capture is automatic… no manual saves… every page you open becomes a stored, citable source," but capture is a one-click action (`captureCurrentPage`, reachable only from the Brief Capture sheet and project panel) and the ambient-capture machinery (`ScribeCoordinator`, `PageCapture*`, `PageContextBroker`) was never wired. The copy now says "capture in one click" and "pages you capture become stored, citable sources," and the YC pitch's "ambient/automatic" capture claims were aligned to match.

### Certificate-error surfacing + command-palette empty state
- **Hive now knows when a site's certificate fails validation.** CefKit already fired `didEncounterCertificateError` but CefWebViewModel never forwarded it — the site-security popover unconditionally claimed "certificate verified by a trusted authority." A new `onCertificateError` closure (returning whether the load is overridden) forwards the failure to the browser state.
- **Security popover is honest about broken certs**: when the active tab hits an `ERR_CERT_*` failure (-200..-216), the popover header flips to a red "Connection is not trusted" shield, the "Certificate valid" row is replaced by a friendly code-specific warning (expired, name mismatch, revoked, untrusted authority, weak key…), and a **Visit site anyway** button reloads the failed address with the host bypassed for the session (Chrome keeps bypasses non-persistent too).
- The cert handler allows a load without prompting again once the host is bypassed; a new navigation attempt clears the error; failures are tracked per tab, session-local, and never persisted. Private tabs report too.
- Because CEF also reports certificate failures through `OnLoadError`, the branded load-error banner now maps the `ERR_CERT_*` codes to the same certificate-specific wording instead of a generic "page couldn't be loaded" — so the banner stays honest while the popover holds the actionable bypass.
- **Command palette empty state** (Raycast/Spotlight convention): a search that matches nothing previously rendered a blank list with a "0 commands" footer. It now shows a proper empty state (icon + "No matching commands" + a hint) and the footer reads "No results".
- Audits confirmed complete: reader mode already has ⌘⇧R (Safari parity), and find-in-page's highlight-all behavior is inherent in CEF's find API (Chrome has no separate toggle) alongside the existing match-case control.

### Branded load-error banner
- **Failed loads now surface a native banner** (Chrome/Arc-style reinforcement of Chromium's own error page): when a tab's main frame fails to load, the chrome shows a dismissible notice with a friendly error title (DNS failure, timeout, refused, etc.), the failed URL's host, the Chromium error text, and a one-click **Retry** that reloads the exact failed address.
- The load-error callback (`CefWebViewModel.onLoadError`, forwarding CefKit's `didFailLoad`) is new — Hive previously never knew a page failed. Errors are tracked per tab, cleared on the next navigation attempt, and never persisted; private tabs report failures too (a failure isn't browsing data).
- Audits confirmed complete: reader mode already offers a font-family picker (Sans/Serif/Mono, persisted + selection traits), the toolbar layout is fixed-width by design with tab-scroll chevrons for overflow, and the site-security popover's buttons are keyboard-reachable.

### New-tab middle-click + suggestion click-away
- **Middle-click the + button opens a background tab** (Chrome convention): the new-tab button previously only handled left-clicks; middle-click now routes to the same `hive.newTabBackground` path as middle-clicking empty strip space.
- **Suggestion dropdown click-away + blur dismissal** (Chrome omnibox convention): the dropdown previously stayed open when you clicked anywhere else in the chrome; an outside mousedown (or window blur) now closes it, while clicking the address field itself keeps it open and suggestion rows still commit on click.
- Audits confirmed complete: reader font scale/theme/family already persist via UserDefaults with live CSS updates, and tab-group collapse state persists (the chevron swap gives toggle feedback; the full re-render architecture makes a height animation impractical).

### Tab-strip middle-click + ⌘9 last-tab
- **Middle-click on empty tab-strip space opens a new tab in the background** (Chrome/Firefox convention): the strip's auxclick handler previously only closed tabs on middle-click; empty area now routes to a new `hive.newTabBackground` bridge (same `newTab` path as the plus button, `activate: false`). Middle-click on a tab still closes it; double-click empty space still opens a foreground tab.
- **⌘9 jumps to the LAST tab** (Chrome/Safari convention): the native numbered-tab commands registered ⌘1–⌘9 against fixed indexes, so with more than nine tabs ⌘9 picked tab 9 instead of the last one. Now it targets the final tab whenever more than nine are open.
- Audits confirmed complete: double-click empty strip opens a new tab, the address bar already selects-all on click/⌘L (with second-click caret placement), and in-app download feedback flows through the toolbar badge + dock progress rather than a toast (not a Chrome/Safari convention).

### Back/forward history menus (Chrome/Safari convention)
- **Long-press or right-click the back/forward buttons** to jump to a recent committed entry — hold ~600ms (or control-click) on the toolbar chevrons, or right-click them, and pick from the last 12 pages. Implemented on both chrome surfaces: the web-chrome toolbar (a hold that opens the menu suppresses the release-click so the page never navigates on top of it; right-click anchors the menu at the cursor) and the native horizontal toolbar's `.contextMenu`.
- **Per-tab navigation stack tracking**: CEF 148 exposes only the current navigation entry (no enumeration API), so committed loads are recorded per tab from the load-observation path — newest first, capped, forward branches truncated on fresh navigation, private tabs never tracked. Back/forward moves slide entries between the stacks so commits don't duplicate.
- Audits confirmed complete: find-in-page has a match-case toggle (`Aa`), reader mode has A−/A+ size controls with a live point readout, and the native address bar selects all on ⌘L focus.

### Dynamics & animation pass (web chrome)
- **No more re-pop on state broadcasts**: the tab strip only plays its entrance animation for genuinely *new* tabs (JS diffs tab ids across renders); previously every refresh re-triggered `tab-in` on every tab, so any state change re-popped the whole strip.
- **Start-page choreography runs once**: the top-sites grid, recent list, and spaces sections now stagger in with a coordinated entrance (sections rise 300ms, tiles pop in `tile-pop` with 30ms stagger, recent rows fade up) — and only on the first render. Previously the whole page re-ran its 500ms `rise-in` on *every* state broadcast.
- **Micro-interactions**: tab close glyph spins 90° on hover (Chrome/Arc), a hovered tab's favicon lifts, and top-site tiles lift with a soft shadow on hover and press down on click (spring-curved `translateY`/`scale`).
- Motion follows the design system's existing tokens (`--spring`/`--spring-soft`, all durations 180–300ms) and respects the user's animations toggle via the existing `.no-motion` rule.

### Dock-tile download progress (macOS convention)
- **Dock progress bar (Safari/Chrome parity)**: while any download is in flight, the dock icon shows a slim white-on-dark progress bar at the bottom showing the mean progress of the active transfers; it clears the moment the last one finishes, cancels, or fails. The custom content view redraws the standard app icon every frame, so the dock identity is never lost.
- **Dock bounce on completion**: when a download completes while Hive is backgrounded, the dock icon bounces once as the OS-level signal (same gate as the existing completion notification, so it shares the user's download-feedback preference).
- Implementation: new `DockTileProgress` type (icon+bar `NSView` content view, redraws throttled to ~2% steps except at terminal boundaries, main-thread hops inside so the CEF download callbacks can call it safely); hooked into the existing `onDownloadProgress` false→true completion transition.
- Audits confirmed complete: per-site zoom memory already persists via UserDefaults (`HiveSiteZoomLevels`) across relaunches, tab-strip scroll-into-view on activation exists, ⌘L select-all-on-focus exists, and fullscreen exit + native toolbar reveal are handled.

### Status bubble + downloads sections
- **Link-hover status bubble (Chrome convention)**: hovering any chrome surface that leads somewhere — start-page tiles, recent/history/bookmark/download rows, bookmark-bar chips, tab chips — now shows the destination URL in a small bottom-left bubble that clears on leave, window blur, or pointer exit. Previously the destination was only visible as a tooltip after a delay.
- **Downloads panel sections (Chrome parity)**: the panel now groups downloads under Active, Completed, and Failed & Cancelled headers instead of one undifferentiated list; the buckets match the native state strings exactly (completed / cancelled / failed / paused / inProgress / pending) so a canceled or failed file is never shown under a "Completed" header. Live rows keep their pause/resume/cancel controls and progress bars.
- Audits confirmed complete: history rows already group under Today / Yesterday / This Week / Older headers, and the tab strip already supports drag-to-reorder plus external URL/text drops.

### Private-tab badge
- **Private-tab badge (Chrome/Arc incognito parity)**: private tabs now carry a small dark eye chip in the web-chrome tab strip, so they're unmistakable in a mixed strip even when unfocused (previously the DTO carried `isPrivate` but the shell never rendered it). Pinned tabs keep their pin; the badge sits between favicon and title.
- Audits confirmed complete: workspace panel has icon + color pickers per workspace/profile, the Downloads panel groups Active/Completed with counts, and the start-page/Settings search-engine picker covers all engines (segmented control wired to `hive.setSearchEngine`).

### Bookmark editing (title + URL)
- **Edit Bookmark sheet (Chrome/Safari manager parity)**: hover a bookmark row in the manager (or use its context menu) and pick Edit… to change the name and URL in place. Identity, folder placement, and favicon are untouched; the updated record flows through the same encrypted bookmark sync boundary as every other mutation. Save is disabled for empty fields; Cancel/Escape dismisses.
- Audits confirmed complete: start-page density (compact/comfortable with injected CSS), Tab Search shows a live "N tabs" count with keyboard nav, and the bookmarks bar overflow menu from the prior batch.

### Bookmarks-bar overflow menu
- **Bookmarks-bar overflow (Chrome convention)**: with more than 12 bookmarks, the first 12 render as chips and the rest spill into a "»" menu anchored below the bar — nothing is silently unreachable anymore (previously the bar hard-capped at 24 and dropped the rest). The menu reuses the existing context-menu surface; ⌘/⌃/middle-click still opens in background tabs, and the button is keyboard-reachable with a focus ring.
- Audits confirmed complete: tab peek (Arc-style live preview with hover timing + link hover peek), omnibox Tab-completion (completes the highlighted suggestion's URL without committing), history Clear button (panel footer `data-clear-history` with confirmation), and downloads Clear-finished are all present.

### Paste-and-search
- **Paste-and-search (Chrome omnibox convention)**: pasting non-URL text into the address bar now offers "Paste and search for …" — pasting a URL still offers "Paste and go to …". The split uses Chrome's heuristic: explicit scheme, `localhost`, or a bare hostname with ≥1 dot is a URL; anything else is a query. Both pinned above native results, committed with Enter or click.
- Audits confirmed complete: the native page context menu covers navigation, editing, link (new tab/split/copy), image (open/copy/save as), selection (copy/search/Ask Hive), Boost, Reading List, Pinned Apps, screenshots, and page-URL copy. Duplicate tab is reachable from tab context menus; window-close confirm and downloads auto-open are not modern-browser conventions; Reading List sync is a larger cross-device feature noted for a future pass.

### Tab-close undo toast + reader word count
- **Tab-close undo toast (Chrome parity)**: closing a tab in the web chrome now shows a transient "Tab closed — Undo" toast (6s, single at a time); Undo restores the tab through the native last-closed stack (⌘⇧T path). One-tap recovery instead of hunting the context menu.
- **Reader word count (Safari parity)**: the injected reader JS counts the article's words (punctuation-stripped tokens) and reports them over the console bridge (`HIVE_READER_WORDS|<n>`); the Reader Mode bar shows "~N words" next to the host. Active-tab + reader-mode gated so stale reports can't leak across pages.
- Audits confirmed complete: find-in-page has a live match counter ("3/12"/"No matches") with ↑/↓ + ⌘G/⇧⌘G cycling, the zoom chip already resets on click (⌘0), and Reopen closed tab exists in tab menus + ⌘⇧T.

### History hover delete
- **History rows now show a hover ✕** (Chrome convention): delete an entry in one click without opening the context menu. `stopPropagation` keeps the row's click navigation from firing; keyboard-reachable via `:focus-visible` (rows are already tab-focusable).
- Audits confirmed complete: tab-group collapse state persists across sessions (`collapsedGroupTabIDs`), the Settings search-engine picker covers all engines, F2 Quick Commands palette exists, and history rows already had right-click Open/Copy/Delete + arrow-key navigation.

### Reader font family + downloads drag-out
- **Reader font family toggle (Serif / Sans, Safari parity)**: a new `ReaderFontFamily` enum in HiveCore drives the reader stylesheet's `--hive-r-font-family` variable; the Reader Mode bar gains two `Aa` buttons (serif vs rounded sans) next to the theme swatches. Persisted via `HiveReaderFontFamily`, live-updated through the CSS-variable script, with 5 new tests (default serif, per-family CSS stacks, update script, Codable round-trip).
- **Downloads drag-out (Chrome/Safari convention)**: completed, non-canceled downloads whose file exists on disk can now be dragged from the Downloads panel to Finder or another app via `NSItemProvider` with the file URL + suggested name.
- Audits confirmed complete: new-window (⌘⇧N native), tab drag-reorder + active-tab scroll-into-view, and a full web-chrome design pass (hover states, transitions, spring animations, and `:focus-visible` on every interactive surface — no gaps found).

### Reader reading progress + reload context menu
- **Reading progress bar** (Safari/Arc convention): entering Reader Mode now injects a thin accent bar pinned to the top of the viewport that fills as the article is scrolled — styled with the reader theme's link color (`--hive-r-link`), passive scroll/resize listeners, and torn down with the rest of the reader injection on exit.
- **Right-click on Reload shows the Chrome page-action menu**: Reload (⌘R) / Empty Cache and Hard Reload (⌥⌘R), reusing the existing context-menu surface.
- Audits confirmed complete: Ctrl+Tab/Ctrl+Shift+Tab tab cycling (web chrome), per-site zoom persistence (applied on load via `applySiteZoom`, remembered on zoom via `setZoom`), tab drag-reorder + active-tab scroll-into-view, and the zoom % indicator.

### Hard reload, paste-and-go, suggestion-Enter fix
- **⌥⌘R hard-reloads bypassing caches** (Chrome's ⌘⇧R is Reader here, so the ignore-cache shortcut moved to ⌥⌘R): native View-menu item, web-chrome keydown, and command-palette entry, all through a new `hive.reloadIgnoringCache` bridge → `reloadIgnoringCache()` → `CefWebViewModel.reloadIgnoringCache()` → CEF `reload_ignore_cache`. The JS comment claiming CEF reload was "already hard" was wrong — plain reload does not bypass caches.
- **Paste-and-go** (Chrome omnibox convention): pasting a URL into the web-chrome address bar immediately offers a single "Paste and go to …" suggestion, committed with Enter or click.
- **Fixed broken Enter-on-highlighted-suggestion**: `renderSuggest` never assigned `__sugg` to suggestion rows, so pressing Enter with a highlighted row read `undefined.__sugg` and threw — highlighted-suggestion navigation via keyboard silently did nothing. Rows now carry their backing suggestion object.
- Shortcuts overlay documents ⌥⌘R; palette gains "Reload Ignoring Cache (⌥⌘R)".

### Reader, copy-URL, fullscreen-exit, bookmarks-bar clicks
- **⌘⇧R toggles Reader Mode** (Safari convention) from the native View menu and the web-chrome keydown — the first keyboard path to Reader besides the toolbar button.
- **⌘⇧C copies the current URL** (Chrome convention) from the native View menu and the web-chrome keydown, via a new `hive.copyPageURL` bridge (`copyPageURL()` no-ops on chrome/blank pages).
- **Esc now exits fullscreen** — the last resort in the web chrome's Escape chain, backed by a safe no-op `hive.exitFullscreen` bridge that only toggles when the window is actually fullscreen (macOS `styleMask` check).
- **Bookmarks-bar chips honor modifier clicks**: ⌘/⌃/middle-click opens the bookmark in a background tab, plain click navigates the active tab (Chrome bookmarks-bar convention).
- **Esc-handling review fixes**: closing the command palette (or any overlay) with Esc can no longer also exit fullscreen, and the address bar's two-stage Esc is no longer cut short by the document-level chain — both fixed by stopping propagation at the consumers. ⌘⇧R is now safe on any page: `toggleReaderMode()` guards on `canUseWebPageActions` at the source, so reader CSS can never be injected into chrome/start/blank pages. The reload keydown excludes Shift so ⌘⇧R can't fall through to a reload.
- Shortcuts overlay documents ⌘⇧R and ⌘⇧C.

### Address bar — favicon + two-stage Esc (Chrome omnibox parity)
- The **page favicon now leads the web-chrome address bar** (Chrome omnibox convention): a real http(s) page with a favicon shows the site icon; the lock glyph only appears when no favicon is available or the connection isn't secure. Clicking the icon still opens site settings. Falls back to the lock glyph if the favicon fails to load.
- **Two-stage Escape in the address bar**: a first Esc closes the suggestion list and restores the page URL; a second Esc blurs the field (matches the native bar's behavior and Chrome's omnibox).
- Audits confirmed complete: web-chrome tab context menu (Pin/Duplicate/Close others/Copy Link/Reopen closed), native History/Bookmarks/Tab-Search/Reading-List panels, bookmarks-bar folder filtering (folders are structural and never render as broken chips), and downloads "Clear finished" needing no confirm (files stay on disk).

### Find in Page — full Chrome/Safari parity
- The find bar now shows a **live match counter** ("3/12", orange "No matches") reported back from the page over the existing console bridge (`HIVE_FIND|<current>|<total>`), gated to the active tab. The selection jump (`window.find`) stays instant; the counter's full-page text walk is debounced 120ms so fast typing on long pages stays smooth.
- **⌘G / ⇧⌘G** (Find Next / Find Previous) added to the native Edit menu; **↑/↓ inside the find field** move between matches (Chrome convention); **Aa "Match case" toggle** re-runs the find immediately and is honored by every path (typing, ⌘G, chevrons).
- ⌘F/⌘G reopen the bar with the last query (Chrome behavior); find execution moved onto `BrowserState` (one entry point for typing, chevrons, menu items, Escape, close).

### Chrome conventions — context menu, tabs, address bar
- Page **context menu now heads with Back/Forward** (shown only when the tab actually has history — CEF can't gray items), standard Chrome menu order.
- **Tab loading spinner**: while a page loads, the tab's favicon slot shows a spinner that swaps to the letter-avatar/favicon on completion.
- **Native ⌘L now selects the whole URL** (Chrome convention — typing replaces it), matching the web chrome's ⌘L handler; a plain mouse click still places the caret.

### Home button, Clear Browsing Data, downloads context menu
- **Home button** added to the web-chrome toolbar (house icon between Forward and Reload) plus native **Go to Home Page (⌘⇧H)** and web-chrome ⌘⇧H — all navigate the active tab to the Home destination (Morning Brief or start page per the new-tab preference; private tabs always get the redacted start page via `goHome()`). ⌘⇧H (not ⌥⌘H) avoids colliding with macOS's system "Hide Others" binding.
- **Clear Browsing Data… (⌘⇧⌫)** added to the native History menu and the web-chrome keydown, opening the native sheet via a new `hive.openClearData` bridge.
- **Downloads rows get a right-click menu** (Chrome parity): Open / Show in Finder (completed), Copy Link, and Remove from List — backed by a new `removeDownload(id:)` + `hive.removeDownload` bridge. In-flight downloads are never removable from view (cancel via the row control instead).
- **Fixed a real download-panel bug**: the web-chrome DTO never projected the paused state (native sends `completed|cancelled|failed|inProgress|pending`), so a paused download rendered as "in progress" with a pause button instead of a resume button. The DTO now emits `paused` from `controlState.state`, and the new context menu keys off the exact native state names (`cancelled`/`failed`, double-l).
- Shortcuts overlay documents the new ⌘⇧⌫ and ⌘⇧H keys.
- Audit: all 63 `hive.*` bridge calls made by the web chrome have matching native registrations — **zero dead controls**.

### Web Chrome — Shortcuts overlay completeness
- Shortcuts overlay now documents **⌘P** (Print) and **⌘.** (Stop Loading).

### Web Chrome — Palette actions & review fixes
- **Command palette gains a Page Actions section**: Reload, Stop, Reader Mode, Fullscreen, Print…, and the three zoom commands — all reachable without touching the toolbar.
- **Review fixes**: the confirm modal's Escape no longer also closes the panel behind it; type-to-search no longer types behind an open modal/overlay; detached palette section headers are hidden when their items don't match; `dataset.index` no longer miscounts separators; the confirm button label is configurable.

### Web Chrome — Print & Stop parity
- **⌘P prints the active page** from the web chrome (new `hive.printPage` bridge → native `window.print()` dialog).
- **⌘. stops the current load** from the web chrome (matches the native Stop menu item; the toolbar's reload↔stop button already used `hive.stop`).

### Web Chrome — Review fixes (type-to-search, confirm modal)
- **Type-to-search no longer swallows `?`** (the shortcuts overlay keeps its key) and **appends** to existing query text instead of replacing it.
- **Panel-row Enter/Space never steals activation from row controls** (bookmark ×, download pause/cancel/reveal buttons handle their own keys).
- **In-page confirmation modal** for Clear History — `window.confirm` is a no-op in this CEF embed (no OnJSDialog handler), so the destructive wipe was previously unconfirmable. Focus is trapped on the danger button; Esc/backdrop cancels.

### Web Chrome — Strip wheel-scroll & ⌘⇧B
- **Wheel over the tab strip scrolls it** (Chrome parity, horizontal mode; event bubbles when at the ends).
- **⌘⇧B now toggles the bookmarks bar** (it previously opened the bookmarks panel like ⌘B — the overlay already documented the toggle).

### Web Chrome — Peek actions & destructive confirm
- **Tab-peek Switch/Close buttons are now live** — they rendered but had no handlers; hovering a tab's preview now actually switches or closes.
- **"Clear history" requires confirmation** (Chrome convention for destructive wipes).

### Web Chrome — Keyboard & NTP parity
- **Start page "type anywhere to search"** (Chrome NTP convention): a printable key with no modifiers focuses the stage query and inserts the character.
- **Panel row keyboard navigation**: history/bookmark/download rows are focusable; ↑/↓ move, Enter/Space open.
- **⌘⌥1-9 space switching** implemented in the web chrome (matches the native menu, documented in the shortcuts overlay).

### Web Chrome — Context-menu & download feedback
- **Context menus now dismiss on click-away, window blur, and Escape** — previously they stayed open until an item was chosen.
- **Download-complete toast**: the web chrome announces a finished download via toast (once per file), matching Chrome/Arc behavior.

### Web Chrome — Toolbar & small-feature fixes
- **Fixed dead controls**: the toolbar theme button did nothing — it now cycles System → Light → Dark with a toast, and the sun/moon glyph swaps with the active theme.

### Web Chrome — Settings parity & History search
- Web-chrome Settings panel now mirrors the native **AdBlock** (`hive.setBrowserPref` key `adBlock`) and **Memory Saver** (`memorySaver`) toggles — the same UserDefaults-backed properties the native Privacy/Performance scenes bind.
- Fixed malformed Settings HTML: the bookmarks-bar toggle's trailing `;` discarded the Layout section's closing `</div>`.
- **History panel search** (Chrome ⌘Y parity): a sticky filter box with live title/url/host filtering, keep-day-grouping, empty-state, and a clear button.

### Web Chrome — Reader, Fullscreen & Zoom
- Toolbar now offers **Reader Mode** (new `hive.toggleReaderMode` bridge; button only shows for http(s) pages) and **Fullscreen** (existing bridge exposed in UI).
- `WebChromeTab` gains `isReaderMode`; `⌃⌘F` fullscreen shortcut works from the web chrome.
- **Page zoom** from the web chrome: `⌘+`/`⌘-`/`⌘0` now reach the native zoom ladder (new `hive.zoomIn`/`hive.zoomOut`/`hive.resetZoom` bridges), with a live **zoom % chip** in the toolbar that only appears when zoom ≠ 100% and resets on click.
- **Fixed**: the reload↔stop toggle and address-bar progress never activated — the JS read a nonexistent `active.loading` field instead of `isLoading`. The stop square, Esc-to-stop, and loading progress bar now work.


All notable changes to Hive Browser will be documented in this file.

Format based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Hive adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased] — 2026-08-09

### Added
- **HTTPS-Only toggle** (web-chrome Settings panel, Chrome parity) — the Privacy section now mirrors the native row: "Always use secure connections" routes through a new `hive.setHTTPSOnly` bridge into the same UserDefaults-backed `isHTTPSOnlyEnabled` property (turning it off dismisses any active warning, matching native).
- **Default search engine selector** (web-chrome Settings panel, Chrome parity) — pick DuckDuckGo, Google, Bing, Brave Search, Startpage, or Ecosia; the new `hive.setSearchEngine` bridge mirrors the native Settings → Search row (same enum, same `scheduleAutosave`), and the start data carries the current engine to the shell (private and normal start pages included).
- **Open File for completed downloads** (web-chrome Downloads panel, Safari parity) — completed rows now offer both **Open file** (`hive.openDownloadFile` bridge → `NSWorkspace.open`) and **Show in Finder**, mirroring the native chrome's hover actions.
- **History & bookmark row context menus** (web-chrome panels, Chrome parity) — right-click a **History** row for **Open in New Tab / Copy URL / Delete Entry** (the latter via a new `hive.deleteHistoryItem` bridge keyed on a durable `historyID` the DTO now carries; it stages the same sync tombstone as the native per-item delete). Right-click a **Bookmark** row for **Open in New Tab / Copy URL** alongside the existing × remove.
- **Tab Search (⌘⇧A) and Find in Page (⌘F) from the web chrome** — two new token-gated bridges (`hive.openTabSearch`, `hive.openFindBar`) surface the native SwiftUI overlays while the shell has focus. This also fixed a real keyboard collision: `⌘A` (Ask Hive) previously swallowed `⌘⇧A` (Tab Search), and `⌘F` never reached the find bar. The shortcuts overlay now documents the actual bindings (`⌘A` Ask Hive, `⇧⌘A` Search Tabs, `⌘⇧J` Downloads) instead of the incorrect `⌘J` claim.
- **Tab-key address-bar autocomplete** (web chrome, Chrome omnibox parity) — pressing **Tab** while suggestions are open completes the field with the highlighted suggestion's URL (or the first suggestion), without committing navigation; the field re-selects so typing replaces the completion.
- **"See more" on Recently Visited** (start page, Chrome NTP parity) — expands the six-row preview to the full start-data history list (the page already carried `state.history`), then hides itself. Middle/⌘-click conventions apply to the expanded rows too.
- **"Clear finished downloads"** (web-chrome Downloads panel, Chrome parity) — a new `hive.clearFinishedDownloads` bridge (shell-token-gated) routes into the same terminal-row filter as the native chrome; the panel refreshes from the native snapshot.
- **Show in Finder for completed downloads** (web-chrome Downloads panel, Safari parity) — completed non-canceled downloads with a file on disk get a **folder action** that reveals the file in Finder (`hive.revealDownload` bridge → `NSWorkspace.activateFileViewerSelecting`). The DTO carries an honest `hasDestination` flag so the button only appears when the file actually exists.
- **Chrome-style history grouping in the web-chrome History panel** — entries are bucketed under **Today / Yesterday / This Week / Older** headers (sticky, uppercase, matching the native HistoryPanel's buckets via a shared `historyDayLabel(for:)` helper on `WebChromeRecentItem`). The start-page "recent" rows and the history search bridge carry the same day label.
- **Download controls in the web-chrome Downloads panel** (Chrome parity) — live rows now offer **Pause / Resume / Cancel** buttons (three new shell-token-gated bridges routing into the same per-download state machine and CEF controllers as the native chrome): an active download may pause or cancel, a paused one may resume or cancel, and terminal rows are immutable. The panel refreshes from the native snapshot after each action.
- **Start-page search suggestions keyboard UX** — the new-tab search dropdown now supports **↑/↓ to move through suggestions** (with the same active-row highlight as the address bar) and **Enter picks the highlighted row**, plus Esc clears both the highlight and the dropdown.
- **Tab-group header context menu** (web chrome, Chrome/Arc parity) — right-click a group header for **Rename** (focuses the inline input), a **color palette** of eight presets (recolor via the existing `hive.setTabGroupColor` bridge, active color ringed), or **Ungroup** (dissolves the group via `hive.deleteTabGroup`).
- **Omnibox TLD shortcuts** (Chrome parity) — **⌘⏎** appends `.com`, **⌘⇧⏎** `.org`, **⌥⌘⏎** `.net` when the address-bar input is a bare hostname (no scheme, no dot, no spaces — exactly Chrome's rule; a highlighted suggestion always wins over the modifier).
- **History/Bookmark panel new-tab conventions** (web chrome, Chrome/Safari parity) — **middle-click or ⌘/⌃-click** on a History or Bookmarks row opens it in a **new background tab** (panel stays open), matching the start-page and omnibox-suggestion behavior; plain click still navigates in place.
- **Tab-peek edge handling** (UI fix) — the hover preview card now flips to the LEFT of the tab when it would cross the window's right edge, drops BELOW the tab in the horizontal strip layout, and clamps to the viewport in both axes (it previously overflowed off-screen for tabs near the right edge and anchored wrongly in strip mode).
- **Ctrl+Tab / Ctrl+⇧Tab tab cycling** (Chrome/Arc parity) — cycles through non-pinned/non-essential tabs with wrap-around and a transient "Next/Previous tab" toast for visual feedback (⌘⇥ remains the OS's app switcher, so the browser uses Ctrl). Works in both chrome layouts, including while the address bar or a panel has focus.
- **External-scheme handoff** (Chrome/Safari parity) — clicking a `mailto:`, `tel:`, or `sms:` link now opens the **default OS app** (Mail, FaceTime, Messages) instead of loading an error page in the browser. CEF's before-browse policy hook is now surfaced as an `onNavigationPolicy` delegate on `CefWebViewModel`; a pure HiveCore `ExternalSchemePolicy` (8 tests) classifies the scheme (case-insensitive, minimal allowlist), the load is cancelled, and `NSWorkspace` opens the URL. Private tabs hand off identically — no browsing data is ever recorded for a handed-off URL.
- **Mute Tab (⌘⇧M)** — Chrome's menu-bar shortcut now toggles the active tab's renderer mute (the label flips Mute/Unmute), one more keyboard path onto the single `toggleMuteTab(id:)` source of truth shared by both chrome layouts, the tab menus, tab search, and the mini-player.
- **Tab audio indicators + downloads badge** (web chrome, Chrome/Arc parity) — playing tabs show a speaker badge and muted tabs a crossed speaker in the web-chrome tab strip; clicking either toggles renderer mute via the new shell-token-gated `hive.toggleTabMute` bridge (same `toggleMuteTab(id:)` source of truth as the native chrome). The Downloads toolbar button gains a **live count badge** while any download is in progress or paused, clearing when the panel is opened and none remain.
- **Native chrome tooltips** — hover tooltips now cover every native chrome control: back/forward/reload-stop, new tab, the shared `iconButton` helper (summarize, reader, AI, knowledge, studio, brief, approvals, customize, layout), profile/workspace menu, memory-saver leaf, command palette, and group disclosures in both chrome layouts (shortcut glyphs included, e.g. "Reload (⌘R)").
- **Top-site shortcut menus** (Chrome NTP parity) — right-click a start-page top-site tile for **Open in new tab** (background) or **Remove from top sites** (persisted natively in UserDefaults via a new `hive.hideTopSite` bridge; removed hosts are replaced by the next-most-visited sites). Tiles are now focusable with **Enter/Space to open**.
- **Close Window (⌘⇧W)** — closes the frontmost window from the File menu, standard browser behavior alongside Close Tab (⌘W). The web-chrome tab context menu also gained **Copy Link** (http/https tabs only, reusing the existing `hive.copyLink` clipboard bridge).
- **Start-page new-tab conventions** (Chrome/Safari parity) — **middle-click or ⌘/⌃-click** on a top-site tile, recently-visited row, or omnibox suggestion opens it in a **new background tab** (plain click still navigates in place); middle-click on a start-page space switches workspace. A new `WebChromeOpenTabRequest` DTO carries an optional `activate` flag (default true, so legacy callers keep foreground behavior) through the http/https-whitelisted `hive.newTabWithURL` bridge.
- **Web chrome keyboard UX** (a11y) — tabs now use a roving `tabindex` with **arrow-key switching** (↑↓/←→ move + select, Home/End jump, Enter/Space select), and workspace dots are focusable buttons with arrow navigation. A comprehensive **`:focus-visible` accent ring** now covers every interactive chrome surface (tabs, nav buttons, workspace dots, bookmark chips, suggestion rows, palette, panels, context menu, start page) so keyboard users always see focus without mouse users seeing rings. The security lock gained a **"Connection is secure/not secure" tooltip**, and the reload button's tooltip swaps to **Stop (Esc)** while loading.
- **Small browser features** (web chrome) — the things a browser just does, now in the chrome shell: **middle-click closes a tab** and **switches workspace**; **double-click empty strip space opens a new tab**; **drag a link (or text) onto the tab strip to open it in a new tab** (validated http/https via a new `hive.newTabWithURL` bridge) or **onto the address bar to navigate**; the **reload button toggles to a Stop square while a page loads** (clicking cancels via the existing `hive.stop`); the **address bar gains a ✕ clear button** while it has text and **selects the whole URL on focus** (⌘L/click convention); and the **security lock opens the per-site Settings hub** for the active page (new `hive.openSiteSettings` bridge). All new bridges are shell-token-gated and http/https-whitelisted like every privileged action; the JS syntax is validated at build.
- **Safety Check** (Chrome `chrome://settings/safetyCheck` parity) — one surface that reviews **passwords** (weak <8-char or single-character-class, plus reused-across-sites detection), **Safe Browsing** (is the API key configured), **extensions** (count with an honest "can't inspect behavior" note), **updates** (is the Sparkle feed wired), **notification permissions** (how many sites may notify), and **HTTPS-only** status. A pure HiveCore `SafetyCheckPolicy` (24 tests) classifies and assembles the Chrome-ordered report; each row opens the most relevant panel (password manager, extensions, site settings, Settings scene, or a live update check), with a **Run again** button and a local-only footer. Reached from ⌘K and Settings → Privacy → **Run Safety Check…**.
- **Site Search Keywords** (Chrome parity) — custom search engines can carry a keyword; typing `kw query` in the omnibox searches that engine directly (resolves before the standard omnibox resolve, shows as the top suggestion, and works on private start pages as explicit user intent). A pure HiveCore `SiteSearchKeywordPolicy` (10 tests) normalizes keywords (trim/lower/no-spaces) and validates templates (http(s) + `{query}` required — the add form now reuses it instead of a weaker inline check), and the omnibox resolver and the search-engine manager share one keyword source of truth. Legacy sessions decode with the new optional `keyword` field untouched.
- **History row actions** (Chrome parity) — right-click any entry in the History panel for **Open in New Tab**, **Copy URL**, or **Delete Entry**. Per-item deletion removes the entry and stages one sync tombstone keyed exactly like Clear Browsing Data, so peers converge on the single-item delete without a full clear.
- **Full-Page Screenshot** (Chrome/Safari parity) — the right-click and ⌘K screenshot actions now offer "Capture Full Page…" and "Copy Full Page": CDP's `captureBeyondViewport` stitches the entire scrollable document into one tall PNG (viewport capture unchanged). The save path flows through a shared helper (Save panel → file → completed download, source URL preserved), full-page saves get a `-full` filename suffix, and the honest doc note covers fixed-viewport and infinite-scroll pages that only capture what's reachable.
- **Task Manager** (Chrome ⇧⎋ parity) — a live table of every Chromium process task with **real** per-process memory and CPU from CEF's process-wide task manager (the C API was wired into the CCef symbol loader after verifying the bundled libcef exports `cef_task_manager_get`; a new `CefTaskManager` Swift wrapper exposes tasks, snapshots, kill, and browser→task mapping). Rows merge CEF task info with the tab they back (title/favicon/active/private badges), sort Chrome-style (browser, GPU, then renderers by title), refresh every two seconds while open, and offer a search filter. "End Process…" (killable tasks only, with confirmation) issues CEF's `KillTask` — a page's tab stays but shows a crashed state until reload, stated honestly in the toast and dialog since the app has no auto-recovery handler. Pure `TaskMetricFormatter` (7 tests) renders memory and CPU with a stable POSIX locale and CEF's percent-of-one-core semantics (200 = two cores, like Chrome). Reached from the **Task Manager…** menu command (⇧⎋), ⌘K, and documented in the Shortcuts panel.
- **Bookmark All Tabs** (Chrome ⌘⇧D parity) — saves every open tab in the current workspace into a new date-named folder (e.g. "Aug 10, 2026") via a pure HiveCore `BookmarkAllTabsPolicy` (deterministic en_US_POSIX folder naming, http/https-only eligibility, case-insensitive batch dedupe — 10 tests). Private tabs, internal chrome pages, and duplicate URLs are excluded; each child flows through the encrypted bookmark sync boundary like any single save, and the outcome lands in the app-wide toast (which was generalized to a shared `showAppNotice(_:)` owner used by Clear Browsing Data, Delete Site Data, and Bookmark All Tabs, so a pending auto-clear can never wipe a newer message). Reachable from the **Bookmark All Tabs…** menu command (⌘⇧D), ⌘K, and both tab-strip context menus.
- **Delete data for this site** (Chrome per-site content-settings removal) — the Site Settings hub's per-host row now offers "Delete data for this site": browsing history for that site is removed with sync tombstones (propagates to synced devices), and its cookies are deleted through the live page's CDP session (pure `SiteDataPolicy` matches history hosts and cookie domains, including leading-dot and subdomain cookie scoping, with each cookie's own path preserved). The confirmation honestly states browser cache can't be scoped per site, and the toast never claims a deletion that didn't happen (no-op when nothing matches, failure-corrected only while it's still the current notice).
- **Site Settings hub** (Chrome `chrome://settings/content/all` parity) — one searchable sheet listing every host with a remembered decision across all four per-site stores: zoom, mute, HTTPS-Only exception, and permission grants (pure HiveCore `SiteSettingsIndex` merges and sorts them). Rows expand inline to set zoom from the standard ladder, toggle "Mute site", toggle "Allow HTTP" (with an honest footnote when HTTPS-Only is off), change any permission Ask/Allow/Block, or reset everything for the site with a confirmation. Reached from ⌘K, Settings → Privacy, and a new Site Settings row in the lock popover that focuses the current page's host. Host-first zoom/mute actions apply to every open tab on the site (hibernated tabs included); the mute toggle shares one implementation with the tab context menu.
- **P2.6 Morning Proactive Briefing** — the brief's proactive card, painting caption, and looking-ahead line are now derived from real Honeycomb memory (captures/notes/briefs/sources/claims since yesterday) via a pure planner, with a once-per-day refresh timer and a Settings toggle.
- **P2.6 calendar half** — opt-in "Include Today's Calendar": an EventKit adapter feeds today's events into the brief's looking-ahead line. Default off, permission requested lazily, empty degradation on every denial path.
- **Zen browser import** — Zen shares the Firefox `places.sqlite` parser and is advertised by `detectAvailableBrowsers` (onboarding + bookmarks manager).
- **Clean Up Tabs** (Arc/Boost parity) — review duplicate (URL-normalized) and stale (30-day) tabs in one sheet; pinned/essential/private tabs are never suggested; `/clean` slash command.
- **Export Bookmarks to HTML** — Netscape bookmark file export with an honest failure alert (Chrome/Safari/Arc parity).
- **Per-tab Mute** — the tab strip's speaker is now a real mute control (CEF `SetAudioMuted`, whole-renderer — works before media plays and survives navigations), wired in both chrome layouts, the tab context menus, tab search, and the mini-player (one source of truth). The previously no-op `/mute` slash command now toggles the active tab.
- **Site Boosts** (Arc parity) — right-click any page and choose "Boost This Site…" to style it with your own CSS. Pure `BoostMatcher` (host patterns: `example.com` exact, `.example.com` + subdomains) builds a CSP-safe injection script (constructable stylesheets, which bypass a page's `style-src`, with a `<style>` fallback). Managed in a new Boosts sheet (Customize panel or `/boosts`), one boost per host, re-applied on every navigation, never in private tabs or web-chrome pages.
- **Live Download Controls** (Chrome/Safari/Arc parity) — the Downloads manager's active rows now expose real Pause/Resume/Cancel. CefKit gained a `CefDownloadControl` wrapper over CEF's download-item callback (fire-and-forget pause/resume/cancel, refcount-owning, dropped on terminal state and browser close), an `isPaused` snapshot field, and delegate plumbing through `CefWebViewModel`. The app drives HiveCore's existing `DownloadControlStateMachine` (request → CEF → reconcile from the native paused bit, with a 3-second bounded-wait fallback), and an explicit cancel is preserved against late CEF updates.
- **Reader Mode typography controls** (Safari parity) — the reader overlay now has A−/A+ text-size controls (0.8–1.4× ladder with a live pt readout) and a four-theme picker (Auto/Light/Sepia/Dark). Pure HiveCore `ReaderStyle` generates the stylesheet (font scale + palette baked as `:root` CSS custom properties) and a live-update script; changes persist in UserDefaults and apply instantly without leaving reader mode. Auto clears only the color vars so the system-dark override wins while keeping in-session font adjustments.
- **Site Permissions** (Chrome parity) — CEF permission requests (camera, microphone, location, notifications, automatic downloads) now route through HiveCore's durable per-site decision model instead of being silently denied. Stored grants auto-resolve; unresolved requests surface a Chrome-style banner (Allow/Block, remembered per host); benign classes (clipboard, storage access) are granted quietly and exotic classes stay denied. CefKit gained a retained `CefPermissionPromptCallback` (async prompt resolution with CEF refcount discipline and `on_dismiss_permission_prompt` prompt-ID forwarding). Private tabs always prompt and never persist answers. The Site Security popover gained per-site Ask/Allow/Block controls for all six kinds plus Reset; the "Pop-ups" control is live via `SitePermissionPolicy.allowsNewWindow` on the window-open path. Decisions persist in the session envelope.
- **Password Manager parity** (Chrome/Safari) — inline **Edit** (site/username/password with reveal, Save/Cancel) per row, **Copy Username** (hover + context menu), and a **strong-password generator**: the dice button fills the add form with a guaranteed-class, unambiguous (no `0/O/1/l/I`) password of configurable length, with a weak-password hint under 8 characters. Pure HiveCore `PasswordGenerator` (injectable RNG, 11 tests) and `CredentialSitePolicy.normalize` (scheme/path stripping, trailing-DNS-dot, lowercasing, 5 tests). Saving now normalizes the site, dedupes by (site, username) updating in place (Chrome behavior), and only touches the visible list after the Keychain write succeeds (honest failure). Editing re-keys the Keychain entry when site/username change (old account key removed). `SavedPassword` gained stable identity across edits; a one-time reconcile pass in session restore normalizes legacy sites and merges duplicates.
- **Saved-credential Autofill** (Chrome/Safari parity) — a self-guarding JS probe injected on http/https pages (never private tabs) detects a username+password form, stamps it with a stable form id, and reports it over the console bridge; the native side matches saved credentials by host via a pure HiveCore `AutofillMatcher` (registrable-domain family rule, bidirectional suffix matching, 8 tests) and surfaces a bottom-center **"Use saved password?"** chip (single-match button or multi-account menu; prefilled username ranks first). Filling is ALWAYS an explicit click — the password is only ever written into the page by that action, with full JS string-literal escaping (backslash/quote/newline/U+2028/U+2029). The probe re-arms on every navigation (login pages usually arrive via redirect); chips never follow tab switches (render-gated + dropped on switch), and dismissing stops nags for the host for the session.
- **Password Save/Update capture** (Chrome parity) — completes the credential lifecycle (capture → save → autofill): the autofill probe now watches for form submission (capture-phase `submit` + Enter in a password field, covering JS-intercepted logins) and reports the submitted credential over the console bridge. A pure HiveCore `PasswordCapturePolicy` (15 tests, reusing `AutofillMatcher`'s registrable-domain family rule) classifies each submission as a **save** (new username on the host family), **update** (same username, different password — preserving the credential's original site, Chrome behavior), or no offer (empty submission or identical to what's stored). The bottom-center **"Save password for <host>?" / "Update password for <host>?"** chip offers Save/Update (the explicit click is the only destination of the password — a Keychain write), **Never for this site** (durable per-host list in UserDefaults), and Not now. The submitted credential is held in memory only while the chip shows — never logged or persisted — and offers drop on tab close/switch/navigation.
- **Reading List** (Safari parity) — the HiveCore `ReadingListEntry` model + cap existed but were never wired; the feature is now fully live. Right-click any http/https page and choose **"Add to Reading List"** (or "Remove from Reading List" when already saved — the menu reflects live state; private tabs never offer it). A pure HiveCore `ReadingListPolicy` (16 tests) normalizes article URLs for identity (fragment stripped, bare trailing slash folded, case-folded) while **storing the latest actual URL** (query/fragment intact so paginated articles open the right page), and re-saving an already-listed article updates its title/favicon in place and moves it to the front — preserving its id, saved-at date, read state, and note (Safari behavior). The list is capped at 500 (`hiveReadingListCap`), oldest first out. The **Reading List panel** (command palette → "Reading List") is a searchable sheet with unread dots, hover actions (mark read/unread, edit note, remove), a per-row context menu, and relative last-viewed times; opening an entry marks it read and navigates. Entries persist in the session envelope (forward-compatible decode, like boosts), are never added from private tabs, and remain outside the encrypted-sync boundary (local-only, like boosts and passwords) until added to the mutation boundary.
- **Bookmark Folders** (Chrome/Safari/Arc parity) — the `Bookmark` model always carried `isFolder`/`parentID` but they were dead: the manager and bar were flat and sync flattened the tree. Folders are now fully live. A pure HiveCore `BookmarkFolderPolicy` (14 tests) provides root/children scoping, recursive descendant enumeration, and **cycle-guarded moves** (a folder can never become its own parent or be moved into its own subtree). The manager gains **breadcrumb navigation** (drill into folders, back to root), New Folder (in the current folder), Rename, and Delete (deletes sweep the whole subtree with an honest "can't be undone" alert); every folder/bookmark row gets a **Move to Folder…** menu. The bookmarks bar shows only root-level items — folders render with a folder icon and drill into the manager; the star-save lands in the folder currently being browsed (Chrome behavior). The encrypted CloudKit sync boundary now carries `parentID`/`isFolder` (backward-compatible `decodeIfPresent` — legacy envelopes decode as root content), folder edits propagate like any bookmark, and a remote folder tombstone **sweeps the folder's descendants locally** (captured before removal, tombstones staged per orphan) so peers never converge to orphaned children. `deleteBookmark` routes folder ids through the subtree-sweeping delete for defense-in-depth; the web-chrome DTO and omnibox suggestions exclude folders (containers, never navigation targets).
- **Pinned Web Apps** (Arc/Sidekick parity) — the HiveCore `PinnedWebApp` model existed but had zero app wiring. Apps are now fully live: right-click any page and choose **"Add to Pinned Apps"** (or "Remove from Pinned Apps" when already pinned — state-aware menu; private tabs never offer it). A pure HiveCore `PinnedWebAppPolicy` (19 tests) gives one-app-per-site identity (http/https only, host case-folded + www-stripped, fragment dropped, non-default ports preserved so `localhost:8080` ≠ `localhost`), name validation (trim + 60-char cap + host fallback), dedupe upserts that refresh an existing app in place (name/favicon/stored URL), and a 24-app cap. New apps get a distinct lowest `sortOrder` so the app you just pinned is actually first in the rail (reviewer-caught: the raw-array prepend didn't survive `sortedForRail` display). The **Pinned Apps panel** (command palette → "Pinned Apps") is a searchable sheet with Pin Current Page (honest "already pinned" feedback), rows with favicons (fallback tile with per-app accent color), hover actions (rail reorder up/down, rename, remove — overlay siblings of the open button), a context menu, and a rename alert; opening an app opens a new tab, stamps `lastUsedAt`, and dismisses the sheet. Apps persist in the session envelope (forward-compatible decode), are never added from private tabs, and remain outside the encrypted-sync boundary (local-only, like boosts, reading list, and passwords) until added to the mutation boundary.
- **Per-site Zoom** (Chrome/Safari parity) — zoom levels are now remembered per domain across sessions via `siteZoomLevels` (UserDefaults-backed). Setting a zoom level (⌘+/⌘−) saves it for the host; navigating to a known domain automatically applies the remembered zoom via `onURLChanged`. Resetting to 100% clears the entry. The existing zoom badge in the horizontal chrome right controls shows the current level.
- **Sync Status Indicator** — a compact native sync status icon in both chrome layouts (vertical bottom bar, horizontal right controls). Shows green (available), blue (syncing with pulse animation), orange (error with diagnostic tooltip), or gray (unavailable). Hover popover shows last sync time and E2E encryption notice. Sync state is tracked throughout the setup/push/pull lifecycle with `syncState` (enum) and `lastSyncDate` on `BrowserState`.
- **Tab Group Manager Panel** — a dedicated native SwiftUI sheet for managing tab groups in the current workspace (⌘K → "Tab Groups"). See all groups with color dots, names, tab counts, and collapse state; create with auto-assigned palette color, rename, recolor (6-color TabGroupPalette), reorder (up/down), collapse/expand toggle, and delete with confirmation. Groups are scoped to the current workspace. New action: `moveTabGroup(id:direction:)`.
- **Profile Manager Panel** — a dedicated native SwiftUI sheet for managing browsing profiles (⌘K → "Profiles"). See all profiles with color badges and workspace counts; create with auto-assigned color/icon, rename, recolor (10-color palette), reicon (20 SF Symbols), and delete with confirmation (removes the profile's workspaces, cookies, and CEF profiles). New actions: `addProfile`, `renameProfile`, `setProfileColor`, `setProfileIcon`, `deleteProfile`.
- **Workspace Manager Panel** — a dedicated native SwiftUI sheet for managing workspaces (⌘K → "Workspaces"). Switch, rename, recolor (10-color palette), reorder, create with auto-assigned color and icon, and delete with confirmation. Previously workspaces only existed as scattered context menus.
- **Tab Overview** (Arc parity) — a visual grid overlay of all open tabs across the current profile's workspaces (⌘K → "Tab Overview"). Per-workspace headers with icon/name/tab-count; each tile shows favicon (or a moon for hibernated tabs), title, host, the group color chip, and audio/mute indicators. Hover highlights, a filter field, and keyboard navigation (↑↓←→ move, ⏎ switch, esc close) with cross-space switching. Reviewer caught and fixed two overlay bookkeeping bugs: selecting a tab from the grid now closes both the list and the grid, and the search list and grid now strictly mutual-exclude one another (no stacked dim overlays).
- **Auto Archive** (§7 cold-tab shelf) — the HiveCore `AutoArchivePolicy` and `ArchivedTab` models existed but had zero app wiring. Cold tabs (untouched 14+ days, the policy's default) now move off the live tab strip into a durable **"Recently Archived"** shelf instead of lingering as dead weight. `AutoArchivePolicy` gained an app-facing `TabInput` overload (skips pinned, essential, private, active, collapsed-group, internal-chrome/blank, and warm tabs — the legacy `BrowserTab` API delegates to it), and a new pure `TabArchiveShelfPolicy` (16 tests total) orders the shelf newest-first and caps it at 100 records. A periodic pass runs every 10 minutes (30-second startup settle, then a generous cadence — unlike hibernation's 60s, the 14-day threshold makes per-minute churn pointless), gated by a new **Auto Archive** toggle in Settings → Performance and the Archive panel. Each archived tab is removed with the same cleanup as a close (navigation attempts, observations, pooled previews, permission prompts, autofill/password chips, media tracking) plus a sync tombstone — but NOT pushed to the ⌘⇧T reopen stack (the shelf is its restore path), and the "browser always has a tab" invariant is preserved. The **Archive panel** (palette → "Archive", `/archive`) is a searchable sheet with restore (reopens in the source workspace, reactivates, removes the record — the same-id push correctly bumps past the archive tombstone in sync) and permanent delete via hover actions and a context menu. The shelf persists in the session envelope (forward-compatible decode) and stays local-only like the other non-synced surfaces.
- **Search Engine Manager Panel** — filterable list of 6 built-in engines + custom engine CRUD with URL template validation; custom engines now usable as default via `activeCustomSearchEngineID`; `searchURL(for:)` routes all search paths through custom or built-in engine; engine deletion clears active selection
- **New Window + URL Copy** — **New Window** (⌘N) opens a second scene window via the existing AppKit notification route; **Copy Page URL** and **Copy All Tab URLs** (workspace-scoped, http-only, private tabs excluded) live in the page context menu and command palette.
- **Page Screenshot** (Chrome/Arc parity) — capture the current page to a PNG via the CDP bridge: **Take Screenshot…** (right-click menu or ⌘K) shows a Save panel with a sanitized page-title filename and records a completed download with the page as its source; **Copy Screenshot** puts the PNG on the pasteboard. Both are gated on a real attached, non-hibernated http(s) page and use a 10s CDP bound so a wedged target can't hang the interaction.
- **Download Completion Notifications** (Chrome parity) — a macOS notification is posted the moment a download finishes, but only when Hive is backgrounded (the in-app Downloads indicator is the feedback when frontmost). One notification per transfer (UUID-keyed identifier so re-delivery overwrites), lazy one-time authorization request, and quiet failure on denial. A **Download Notifications** toggle in Settings → Performance (UserDefaults-backed, default on) gates the feature.
- **Keyboard Shortcuts reference panel** — a searchable sheet (⌘K → "Keyboard Shortcuts") documenting every registered shortcut, grouped by category (Tabs & Windows, Navigation, Zoom & View, Workspaces & Split View, Tools & AI) with keycap-chip rendering and a live count footer. The catalog mirrors the actual `keyboardShortcut` registrations in `BrowserCommands` so it can't drift from the menu structure.
- **Downloads Status Indicator** (Chrome parity) — the chrome's downloads button is now a live status indicator: an aggregate progress ring with a count badge while transfers run (hover for a mini popover listing active downloads with per-file progress), a green filled icon when finished downloads exist, and the plain icon when idle. Clicking opens the Downloads panel. Wired into both the vertical bottom bar and the horizontal right controls. Popover dismissal is delay-guarded so it doesn't flicker when the cursor crosses onto it.
- **Clear Browsing Data** (Chrome parity) — a dedicated sheet (⌘K → "Clear Browsing Data…", Settings → Privacy → Clear Browsing Data…) for removing browsing data in one deliberate step. Pick a time range (Last hour / 24 hours / 7 days / All time) and the categories to clear, with **live counts**: browsing history (range-filtered, per-item sync tombstones — cleared history propagates to synced devices), download history (completed records only — in-flight transfers are never touched), cookies & site data, and cache (both via new CDP `Network.clearBrowserCookies`/`clearBrowserCache` calls, browser-global, honestly worded as "via live page" and skipped with an explanation when no web page is open — a failed CDP call corrects the toast instead of silently claiming success). A pure HiveCore `ClearBrowsingDataPolicy` (4 tests) owns the range arithmetic and labels. The confirm dialog is explicit ("Clear selected browsing data?"), the session is autosaved only when something actually changed, and a transient toast reports the outcome. The panel footnote states the honest scope: the time range applies to history — our download records carry no timestamps and CDP offers no time-scoped cookie/cache clearing.
- **HTTPS-Only Mode** (Chrome "Always use secure connections" parity) — a Settings → Privacy toggle (default off) that upgrades plaintext `http://` addresses to `https://` at every app-controlled navigation entry point (omnibox, floating bar, links opened from the chrome, new-tab destinations) while preserving path, query, port, and fragment. A pure HiveCore `HTTPSOnlyPolicy` (6 tests, reusing the shared http(s) host-keying primitive) decides upgrades and warnings deterministically; pages that still arrive over plaintext http — typically in-page link clicks, since CEF exposes no before-browse hook — surface a Chrome-style **"Connection is not secure"** banner with **Use HTTPS**, **Load anyway** (durable per-site exception, managed in Settings), and dismiss. The banner follows the active tab across switches, clears when the page navigates to https or internal chrome, and clears the moment the mode is turned off. Applies to private tabs too (a transport preference, never content); upgrades can't double-fire on already-secure URLs.
- **Copy All Tabs as Markdown** — right-click any page and choose **"Copy All Tabs as Markdown"** (or ⌘K) to export the current workspace's tabs as a markdown link list ready for notes, docs, and handoffs. Same exclusions as Copy All Tab URLs (private tabs, non-http pages), plus hibernated tabs now contribute their wake URL through a shared `workspaceCopyableTabs()` candidate helper so the two copy variants can't drift. A custom tab name wins over the page title (host fallback), labels collapse whitespace runs and escape `[`/`]`, and destinations are wrapped in angle brackets — so titles with newlines and URLs containing `)` or spaces still paste as valid markdown.
- **Group Similar Tabs** (Arc parity) — ⌘K → "Group Similar Tabs" auto-organizes the current workspace: every domain with 2+ ungrouped tabs becomes one tab group, auto-named from the host (e.g. `Github.com`) and colored from the palette. A pure HiveCore `SimilarTabGroupPolicy` (7 tests, reusing the shared http(s) host-key primitive so `www.` and bare hosts merge) orders groups largest-first; the app filters candidates to non-private, non-pinned, non-essential, ungrouped tabs and groups hibernated ones via their wake URL. Re-runs merge new tabs into the existing same-named group instead of duplicating it, the palette cycles off the current workspace's group count (consistent with every other group-creation site), and the group assignments are persisted explicitly so a force-quit right after grouping can't lose them. A transient toast confirms the result ("Grouped 6 tabs into 2 groups" or "No similar tabs to group"), auto-clearing after a few seconds.
- **Mute Site** (Safari/Chrome parity) — right-click any tab and choose **"Mute Site"/"Unmute Site"** to mute a whole domain persistently, layered on top of per-tab mute. A pure HiveCore `SiteMutePolicy` (5 tests) keys sites by the same www-stripped lowercase host convention as per-site zoom (http/https only — internal chrome and local files can't be muted), and `matchesHost` finds every open tab on the host. Muting sweeps all live tabs on that host (hibernated ones included via their wake URL, matching the `duplicateTab` convention) and records provenance in `siteMutedTabIDs`, so **Unmute Site releases exactly the mutes it created** — independent per-tab mutes survive (Chrome's layered model). The mute re-applies automatically on navigation onto the host and on browser attach after hibernation/lazy restore, and the `/mute` slash command now routes to site mute to match its "Mute / Unmute Site" registry title. The host set lives in UserDefaults (a preference, not browsing data — private tabs participate and it never leaves the device).
- **Tab Rename** (Arc/Safari parity) — right-click any tab in either chrome layout and choose **"Rename…"** (or ⌘K → "Rename Active Tab…") to give it a custom name. A pure HiveCore `TabRenamePolicy` (6 tests) normalizes the input: whitespace runs collapse to a single space and names cap at 60 characters; an empty name clears the rename so the page title returns. Custom names win in every tab surface — native strips, tab search (matching included), the window title, and the web-chrome tab strip via the DTO — and survive session save/restore through a backward-compatible `CodableTabInfo.customTitle` (old session files decode unrenamed), duplicate tab copies the name, and reopening a closed tab keeps it. Renames are session-scoped for private tabs (never serialized, like all private-tab state). The rename prompt is a window-level alert shared by both chrome layouts; triggering is native chrome + palette (the web tab strip displays renamed titles but does not yet offer the rename gesture).
- **Memory Saver panel** (Chrome parity) — a dedicated sheet (⌘K → "Memory Saver") for manual tab sleep/wake management on top of the periodic hibernation pass. See every tab's sleep state with a filter field, per-row Sleep/Wake controls, a **Sleep Inactive** bulk action, **Wake All**, and a live **Auto** toggle for the periodic pass. Manual sleeps respect the same safety boundary as the auto pass: private, pinned, essential, active, split, media-playing, and active-download tabs are never candidates (protected rows render an explanatory label), and the bulk action additionally honors the MRU keepalive set so the most-recently-used renderers stay warm. Panel-driven hibernation persists through the same crash-safe session-save path as the periodic pass.

### Fixed
- The web-chrome tab strip now keeps the active tab in view when switching tabs by keyboard (⌘1-9, arrows, workspace moves) in both layouts — previously only tab-set changes scrolled.
- Removed a stray `&lt;` text node in the start-page HTML that rendered as a visible artifact above the particle canvas.
- Durable Honeycomb claim corrections now persist updated content hashes.
- Hash collisions are rejected without partial mutation or phantom revisions.
- Honeycomb revisions, node writes, and FTS replacement now commit transactionally with explicit rollback errors.

### Validation
- 1,782 tests / 174 suites pass; HiveCore and Hive product builds pass; ad-hoc bundle, preflight, and smoke readiness all green.


## [v1.0.0] — 2026-08-09 (build 100)

### Added
- **1349 tests** / 144 suites (+267 from 1082 baseline) — ALL PASSING
- **Chromium 148 backend** via CefSwift — native SwiftUI chrome shell
- **On-device AI** via Apple MLX — Model Council V2 (parallel multi-model with chair synthesis)
- **Agentic Browsing** — CDP-powered, 16-tool bridge surface with timeout guard
- **Deep Research** — multi-step plan/search/read/synthesize with live streaming
- **Zen CSS** (~15K chars ported): 100pt linear() easing, floating URL bar, compact mode, workspace dots, tab peek
- **Brave CSS** (~3K chars): Shields drop-down panel with stats grid
- **Dia CSS** (~7K chars): Typography (vw-based clamp), translucent card ::before bleed, chat swell/pile, dark mode, tab color tokens (18 variables from Assets.car)
- **Comet CSS** (~3K chars): Sidecar slide-in panel, collapsible reasoning chains, step status badges
- **Sidecar JS Bridge** — toggleSidecar() with Agent/Context/History tabs + addChainStep/Chain
- **Sidecar Swift Bridge** — hive.sidecar.open/close APIs + WebChromeSidecarStep/Chain
- **CI/CD Pipeline** — build + preflight + smoke + session-recovery all PASS
- **Vertical + horizontal tab layouts** with Arc-style compact mode
- **Workspaces/Spaces** — per-workspace profile isolation
- **Split view** with draggable dividers
- **Tab peek** — Arc-style hover preview + link peek
- **Private browsing** — ephemeral CEF profiles
- **Crash-only session persistence** — session.json + session.prev.json with recovery notices
- **Command palette** + tab search + floating URL bar
- **Sparkle 2 auto-updates** — graceful degradation for ad-hoc builds
- **Landing page** — web/index.html with download CTA
- **Waitlist, Privacy, Terms pages**
- **Product Hunt page** — web/ph.html with bento feature grid + founder note
- **Open Graph / Twitter Card meta tags**

### Changed
- HiveChromium → Hive — single executable target
- ChromiumBrowserState → BrowserState — all references unified
- Sources/HiveChromium/ → Sources/Hive/
- CSS styles.css: ~2,000 → ~31,000 chars (ported patterns from 5 browsers)
- WebChromeHandler: 60+ bridge registrations including full agent tool surface

### Fixed
- Duplicate start tab — setupDefaults() no longer double-calls newTab()
- ERR_UNKNOWN_URL_SCHEME — scheme handler replay on per-profile contexts
- CDP timeout guard — 15s timeout prevents AI loop deadlocks

### Security
- Keychain secret storage, CEF sandbox, Safe Browsing, EasyList blocking
- DevTools port #if DEBUG-gated, no secrets in repo

### Browsers Studied
- Zen (MPL-2.0): compact mode, tab peek, workspace dots, 100pt easing, URL bar
- Astro (MIT): CDP protocol codegen, browserOS agent architecture
- Brave (MPL-2.0): Shields panel, adblock patterns
- Dia.app: Typography, translucent cards, chat swell, dark mode, color tokens
- Comet.app (Perplexity): Sidecar agent panel, Chromium 150, SSE+WebSocket

[v1.0.0]: https://github.com/arpituppal2/Hive/releases/tag/v1.0.0
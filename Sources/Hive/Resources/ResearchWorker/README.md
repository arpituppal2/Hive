# Research worker release resource

The release pipeline writes `hive-fetch-worker` and
`hive-worker-requirement.txt` here. Both are generated and ignored. Do not
commit a worker binary, a Developer ID certificate name, a Team ID, or a
requirement placeholder.

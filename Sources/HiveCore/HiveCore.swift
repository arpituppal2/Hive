import Foundation

// HiveCore — shared logic for The Hive Browser.
// No external dependencies. Local-first, memory-first.

public enum HiveCore {
    /// Rebuild baseline marker. Bump when the foundation materially changes.
    public static let version = "0.0.0-rebuild"
}
